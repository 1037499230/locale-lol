#!/bin/sh
set -e

# Set SKIP_AUTO_VERSION_BUMP=1 when a version bump is intentionally not wanted.
if [ "${SKIP_AUTO_VERSION_BUMP:-}" = "1" ]; then
  exit 0
fi

node scripts/bump-version.cjs
git add package.json
